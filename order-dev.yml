spring:
  application:
    name: order
  datasource:
    username: root
    password: haohaizi134
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/springclould?characterEnconding=utf-8&useSSL=false&serverTimezone=UTC
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update
server:
  port: 8866
env:
    dev0.0.0.0
